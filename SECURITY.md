# 安全与人工决策边界

QualityTrace 8D 默认完全离线运行，不需要模型凭据，也不会连接真实 MES、ERP 或供应商系统。

## 数据边界

- 仓库中的 case、内部规则、历史案例和证据均为固定 seed 生成的原创合成数据；
- 不包含真实企业、工厂、供应商、员工或客户信息；
- 本地 SQLite、运行报告、trace 和 outbox 写入 `artifacts/`，该目录不进入 Git；
- `.env`、凭据、证书、抓包和本地 Agent 状态由 `.gitignore` 排除。

## 权威写操作

模型或规则模块只能整理证据、提出根因候选和生成措施草稿。纠正措施写入 outbox 前必须存在 `quality_manager` 的持久化批准；结案前必须存在有效性证据和第二次人工决定。

`case_id + action_version` 幂等键防止工具重试或重复点击产生重复措施。证据缺失、冲突、对象不一致或权限不足时，工作流暂停或阻断，不会猜测推进。

## 报告安全问题

不要在公开 Issue、截图或日志里粘贴真实业务资料或凭据。若使用者自行接入外部模型或业务系统，需要单独实现身份认证、最小权限、数据脱敏、写操作确认、审计和回滚；这些能力不属于当前本地 POC。
